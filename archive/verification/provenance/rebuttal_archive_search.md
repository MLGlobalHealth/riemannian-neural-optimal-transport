# Bounded source-archive search — 2026-09-08

No new source archives were found on nvidia6, nvidia7 or dide within the
requested search scope. Consequently no archive extraction, member-text read,
or execution was performed.

The search considered filenames containing, case-insensitively, `riemann`,
`rebut`, `mlgh`, `rcnf`, `moser`, `so3`, `se3`, or `neural` followed by `ot`,
with suffix `.zip`, `.tar`, `.tar.gz`, or `.tgz`.

Only the following previously identified user-owned code/project roots were
searched, with at most six directory levels below each root:

| Server | Existing roots searched | Matching archives |
|---|---|---:|
| nvidia6 | `~/research_projects`, `~/git` | 0 |
| nvidia7 | `~/runs`, `~/entropic-riemannian-neural-ot` | 0 |
| dide | `~/projects`, `~/git`, `~/entropic-riemannian-neural-ot` | 0 |

Immediate home-directory files were also checked for those exact archive-name
patterns, without traversing other home directories. Dataset, cache,
environment, private configuration, `.git`, and bytecode directories were
excluded. Existing verification directories created during this session were
outside the searched project roots. No history/source-content scans were
repeated and no GPU jobs were launched.

Per-host path/bounds records are in `archive_name_scan_nvidia6.json`,
`archive_name_scan_nvidia7.json`, and `archive_name_scan_dide.json`. This route
does not resolve the missing original RCNF/Moser or camera-ready SO3/SE3 KL/ESS
runner location. It does not establish absence outside these stated bounds.
