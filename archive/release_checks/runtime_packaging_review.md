# Public runtime packaging review

**Runtime PASS: no blocking issue found in the declared default workflows.** The subsequent test-relocation issue below is resolved and its archived layout was validated. No implementation, recipe, native-source or test file was changed during this review.

- All 20 focused planner/runtime/plotting/transport tests pass in the existing Python3.11 offline plotting environment. An initial invocation with bare system Python lacked NumPy for two rendering tests; the standard-library tests passed there, and the full suite passed after selecting the documented plotting environment. This was an interpreter dependency issue, not a code failure. No training or JAX computation was run.
- Seven additional isolated restoration checks pass using fake native objects and real NumPy arrays: saved parameters/landmarks/key restoration and refreshed potential-dependent solver/loss/trainer references; rejected parameter shape/dtype, landmark shape/dtype, duplicated checkpoint parameters and saved-configuration mismatches. These checks do not establish real accelerator replay; remote release validation is separate. See `runtime_restoration_review_checks.json` and its small generating script.
- Smoke configuration is applied last, after native/recipe/ablation construction: two outer updates, batch8, two inner steps/min2 and one eight-sample evaluation. Native model initialization and optimizer choices remain intact; smoke is explicit in the result and excluded by plotting aggregation.
- Evaluation calls and random schedules retain the established protocols: main tables train seed+1000 then split2; Lie groups/dimension sweeps fixed12345 then split2; historical ablations fixed12345 then split3 for source/target plus their existing native loss. Only native SE3 evaluation contributes its residual metric. Batch uncertainty is identified separately from across-training-seed uncertainty.
- Checkpoints are saved after synchronized training and before evaluation. Each batch is durably written with native metrics, exact keys and nonfinite flags. Native numerical nonfinite output is retained as a completed numerical outcome; exceptions are recorded as failures. Fresh output directories, full predeclared job manifests, one subprocess/GPU and stop-new-work-on-failure behavior are exercised by tests.
- The staged 80-line CUDA requirements file is byte-identical to the previously committed `verification/environments/reproduction-cuda.txt`, SHA256 `f7b7335f0a864a57487be28384442d962d2519edd54a4c4a21e6382528779171`. Every line is an exact, unique package pin. Core interpreter guards match Python3.11/JAX0.4.35/jaxlib0.4.34/Flax0.8.4/Optax0.2.3; complete installed package versions and actual backend/precision/PRNG settings are recorded. The existing setup must retain its documented CUDA_ROOT export. No install or shared-environment change was performed.
- `source_manifest` scans active src/experiments/rcpm only and tolerates removing obsolete rcpm/requirements.txt. None of run.py, worker.py, _runtime.py or _ablation_native.py imports archive or verification. Historical ablation SHA256 remains `0d13481f1c8f250e8e9c80861bc84ada0e0820a93ec039f60fc64d682f5467ad`; its paper-directory location resolves the active repository src path correctly. Copying tests into archive/release_checks/tests does not introduce a public runtime dependency.

Final native GPU smoke and SE3 parity remain the remote agent's separate validation.

## Test-relocation issue identified after the runtime checks

Moving unchanged tests to `archive/release_checks/tests` breaks two test-only path assumptions: `test_paper_run.py:72` starts its dry-run subprocess in `Path(__file__).parents[1]`, which would become `archive/release_checks` instead of the repository root; `test_plot_data.py:69` would look for reference data under that same archived parent. Anchor these paths to the imported paper module's repository root, or explicitly handle the new test depth. Root has been notified; no test or source changes were made by this review. This does not affect exported/public runtime modules.

## Authorized test-only resolution

Root authorized the two test-only path updates after the read-only review. Both now anchor to `Path(paper.__file__).resolve().parent.parent`. All20 tests passed again in staging and all20 passed from an independent copied `archive/release_checks/tests` layout. No public runtime or native-source changes were made. Updated SHA256 values are retained in `test_relocation_hashes.json`.
